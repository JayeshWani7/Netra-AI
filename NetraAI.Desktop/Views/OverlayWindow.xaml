<Window x:Class="NetraAI.Desktop.Views.OverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Netra AI"
    Height="500"
    Width="400"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    Topmost="True"
    ShowInTaskbar="False"
    ResizeMode="CanResizeWithGrip">
    
    <Border Background="{StaticResource BrushSurface}" CornerRadius="12" 
            BorderBrush="#444444" BorderThickness="1" 
            Effect="{StaticResource ResourceKey=ShadowEffect}">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            
            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource BrushBackground}" CornerRadius="12,12,0,0" Padding="16">
                <Grid>
                    <TextBlock Text="Netra AI Assistant" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Content="×" HorizontalAlignment="Right" Background="Transparent" 
                            Foreground="{StaticResource BrushText}" FontSize="18" Padding="0"/>
                </Grid>
            </Border>
            
            <!-- Chat Area -->
            <ScrollViewer Grid.Row="1" Padding="16">
                <TextBlock Text="Chat area - Ready to help!" Foreground="{StaticResource BrushText}" VerticalAlignment="Center" TextAlignment="Center"/>
            </ScrollViewer>
            
            <!-- Input Area -->
            <Border Grid.Row="2" Background="{StaticResource BrushBackground}" CornerRadius="0,0,12,12" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Grid.Column="0" Background="{StaticResource BrushSurface}" Foreground="{StaticResource BrushText}" Padding="12,8"/>
                    <Button Grid.Column="1" Content="Send" Style="{StaticResource PrimaryButtonStyle}" Margin="8,0,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
    
    <!-- Shadow Effect Definition -->
    <Window.Resources>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/>
    </Window.Resources>
</Window>
